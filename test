import Select from "react-select";

<div className="input-field">
  <Select
    placeholder="Target Environment"
    name="targetEnv"
    options={filteredOptions()}
    closeMenuOnSelect={false}
    menuPortalTarget={document.body}
    required
    isDisabled={isDisabled.targetEnv}
    value={selectedOptions}
    onChange={handlemulti}
    isMulti={true}
    hideSelectedOptions={false}
    getOptionLabel={(e) => e.label} // Ensures only text appears in selected values
    formatOptionLabel={(e, { context }) =>
      context === "menu" ? (
        <div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
          <input
            type="checkbox"
            checked={selectedOptions?.some(opt => opt.value === e.value)}
            readOnly
          />
          <span>{e.label}</span>
        </div>
      ) : (
        e.label // Shows only text in the selected area
      )
    }
  />
</div>;
