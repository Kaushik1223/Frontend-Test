initscript {
    repositories {
        // your maven config...
    }
    dependencies {
        // your dependencies
    }
}

// Add this after the `initscript` block
allprojects {
    tasks.withType(JavaCompile).configureEach {
        sourceCompatibility = '17'
        targetCompatibility = '17'
        options.release = 17
    }
}
