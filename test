import React from 'react';
import './ResultPage.css';

const ResultPage = ({ environmentName, data }) => {
    return (
        <div className="result-page">
            <h2 className="result-title">ConfigDriftValidator - Identifies and highlights differences between environment configurations</h2>
            
            <h3 className="environment-name">{environmentName}</h3>

            <table className="result-table">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Value</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>Total number of properties found</td>
                        <td>{data?.total_properties || 'N/A'}</td>
                    </tr>
                    <tr>
                        <td>Number of Matching properties</td>
                        <td>{data?.matching || 'N/A'}</td>
                    </tr>
                    <tr>
                        <td>Number of Missing properties</td>
                        <td>{data?.missing || 'N/A'}</td>
                    </tr>
                    <tr>
                        <td>Number of Mismatching values</td>
                        <td>{data?.mismatching || 'N/A'}</td>
                    </tr>
                </tbody>
            </table>
        </div>
    );
};

export default ResultPage;
--------------------------------

.result-page {
    width: 80%;
    margin: 40px auto;
    font-family: Arial, sans-serif;
}

.result-title {
    font-size: 22px;
    font-weight: bold;
    margin-bottom: 15px;
    color: #000;
}

.environment-name {
    font-size: 18px;
    font-weight: bold;
    margin-bottom: 10px;
}

.result-table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 10px;
}

.result-table th, .result-table td {
    border: 1px solid #ddd;
    padding: 12px;
    text-align: left;
}

.result-table th {
    background-color: #f2f2f2;
    font-weight: bold;
}

.result-table tr:nth-child(even) {
    background-color: #fafafa;
}

.result-table tr:hover {
    background-color: #f1f1f1;
}
