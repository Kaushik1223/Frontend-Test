import React from 'react';
import './Sidebar.css';

const Sidebar = () => {
  return (
    <div className="sidebar">
      <h3 className="sidebar-title">Config Options</h3>
      <ul className="sidebar-list">
        <li className="sidebar-item">OCP to OCP</li>
        <li className="sidebar-item">TAS to TAS</li>
        <li className="sidebar-item">OCP to TAS</li>
      </ul>
    </div>
  );
};

export default Sidebar;
------------------------------
/* Sidebar Styling */
.sidebar {
    width: 250px;
    height: 100vh;
    background-color: #212121; /* Dark sidebar background */
    color: #fff;
    padding: 20px 0;
    position: fixed;
    top: 0;
    left: 0;
    overflow-y: auto;
    box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
  }
    /* Sidebar */
    .sidebar {
        width: 250px;
        position: fixed;
        top: 65px; /* Below Navbar */
        left: 0;
        bottom: 0;
        background-color: #2c2c2c;
        color: white;
        padding: 20px 0;
        overflow-y: auto;
        z-index: 999;
      }
  
  .sidebar-title {
    font-size: 18px;
    font-weight: bold;
    margin-left: 16px;
    margin-bottom: 20px;
    text-transform: uppercase;
  }
  
  .sidebar-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .sidebar-item {
    padding: 12px 16px;
    font-size: 16px;
    display: flex;
    align-items: center;
    cursor: pointer;
    transition: background-color 0.3s ease;
  }
  
  .sidebar-item:hover {
    background-color: #424242;
    border-left: 4px solid #f7ca3e; /* Yellow highlight on hover */
  }
  ---------------------------------------------
import React from 'react';
import './ResultBox.css';

const ResultBox = ({ result }) => {
  return (
    <div className="result-box">
      <h3>Result</h3>
      <p>{result || 'Results will appear here after submission.'}</p>
    </div>
  );
};

export default ResultBox;
-----------------------------------------------
.result-box {
    background-color: #f9f9f9;
    border: 1px solid #ddd;
    padding: 16px;
    border-radius: 4px;
    width: 35%;
    height: fit-content;
  }
  
