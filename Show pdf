import React, { useState } from 'react';
import uploadIcon from '../assets/upload-icon.png';

const FileUpload = ({ name, value, onFileSelect, disabled }) => {
  const [selectedFile, setSelectedFile] = useState(null);

  const handleFileChange = (event) => {
    const file = event.target.files[0];
    if (file && (file.type === 'application/pdf' || file.type === 'text/csv')) {
      setSelectedFile(file);
      onFileSelect(value, file);
    } else {
      alert('Only PDF and CSV files are allowed.');
    }
  };

  const removeFile = () => {
    setSelectedFile(null);
    onFileSelect(value, null);
  };

  return (
    <div className="file-upload">
      <label htmlFor={`file-input-${value}`} className="file-label">
        <h3>{name}</h3>
        <div className="file-input-container">
          <img src={uploadIcon} alt="Upload Icon" className="upload-icon" />
          <span className="file-input-overlay">
            Drag & Drop or <span className="browse">Browse</span>
          </span>
          <input
            type="file"
            id={`file-input-${value}`}
            className="file-input"
            accept=".pdf, .csv"
            onChange={handleFileChange}
            disabled={disabled}
          />
          <p className="file-support">Supports: PDF, CSV only</p>
        </div>
      </label>
      {selectedFile && (
        <div className="selected-file">
          <h4>Selected File:</h4>
          <ul>
            <li>
              {selectedFile.name} 
              <button onClick={removeFile} disabled={disabled}>X</button>
            </li>
          </ul>
        </div>
      )}
    </div>
  );
};

export default FileUpload;
