

-----------------

import React from 'react';
import TreeNode from './TreeNode';

const TreeView = ({ data }) => {
  return (
    <div className="tree-view">
      {data.Differences.map((account, index) => (
        <TreeNode key={index} node={account} />
      ))}
    </div>
  );
};

export default TreeView;
